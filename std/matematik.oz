// Standart Matematik Modülü

işlev karekok(x) {
    döndür kök(x);
}

işlev ust(taban, kuvvet) {
    döndür üs(taban, kuvvet);
}

işlev mutlak_deger(x) {
    döndür mutlak(x);
}

işlev rastgele_sayi(min, max) {
    döndür rastgele(min, max);
}

işlev yuvarla(sayi) {
    döndür tamsayı(sayi);
}
